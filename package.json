{
  "scripts": {
    "start:db": "npx json-server -w db.json",
    "start:parcel": "npx parcel index.html"
  },
  "dependencies": {
    "axios": "^1.16.0",
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "buffer": "^6.0.3"
  }
}
